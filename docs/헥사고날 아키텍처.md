# 헥사고날 아키텍처

## 1. 주요 구성요소
### 1.1. 포트 (Port)
애플리케이션이 외부와 통신하기 위한 인터페이스
- 인바운드 포트(Inbound Port): 외부로부터 애플리케이션으로 들어오는 요청. 애플리케이션이 할 수 있는 일을 정의. Usecase 인터페이스라고도 함.
```java
interface RegisterMemberUsecase {
    public MemberResponse register(RegisterMemberCommand command);
}
```
- 아웃바운드 포트(Outbound Port): 애플리케이션으로부터 외부로 나가는 요청. 애플리케이션이 필요로 하는 외부 기능을 정의.
```java
interface MemberRepository {
    public Member save(Member member);
}

interface NotificationPort {
    public void sendEmail(String email, String message);
}
```

### 1.2. 어뎁터 (Adapter)
포트가 정의한 인터페이스를 구현한 구현체
- 인바운드 어뎁터(Inbound Adapter): 외부에서 들어오는 요청을 처리. Controller, CLI, Message Consumer 등
- 아웃바운드 어뎁터(Outbound Adapter): 외부로 나가는 요청을 처리. DB, 외부 API, Message Producer 등

> **도메인 메서드 vs Usecase**  
> - 도메인 메서드 (비즈니스 로직)
>   - 단일 객체 내의 상태변경
>   - 비즈니스 규칙 검증
> - Usecase (애플리케이션 로직)
>   - 여러 도메인 객체를 조합
>   - 트랜잭션 경계
>   - 로직 흐름